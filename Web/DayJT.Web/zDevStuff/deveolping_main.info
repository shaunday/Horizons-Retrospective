npm run dev
auto arrange = Shift + Alt + F.
rename all atrl + h



index.html - everything always starts here. vite adds main, which goes to app.jsx ? why all the layers?
margin is for spacing from outside, padding is for spacing for inside (like min size)