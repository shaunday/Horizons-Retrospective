name: Build & Test .NET Projects

on:
  push:
    branches:
      - main
    paths:
      - "./Server.AssetsFlow/**"
      - "./Server.PriceAlerts/**"
  workflow_dispatch:  

jobs:
  Test-AssetsFlow:
    uses: ./.github/workflows/.NET Build CommonSteps.yml
    with:
      server-path: Server.AssetsFlow

  Test-PriceAlerts:
    uses: ./.github/workflows/.NET Build CommonSteps.yml
    with:
      server-path: Server.PriceAlerts
