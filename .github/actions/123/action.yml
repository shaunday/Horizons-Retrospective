name: "Common .NET Build Steps"
description: "Restore, build, and optionally test a .NET project"

inputs:
  server-path:
    required: true
    description: "Path to the server project"
  run-tests:
    required: false
    default: "false"
    description: "Run tests if true"

runs:
  using: "composite"
  steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: "9.0.x"

    - name: Restore
      run: dotnet restore ${{ inputs.server-path }}
      shell: bash

    - name: Build
      run: dotnet build ${{ inputs.server-path }} --no-restore
      shell: bash

    - name: Test
      if: ${{ inputs.run-tests == 'true' }}
      run: dotnet test ${{ inputs.server-path }} --no-build --verbosity normal
      shell: bash
